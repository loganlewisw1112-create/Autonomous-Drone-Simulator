node_modules/
dist/
.env
.env.*
!.env.example
*.local
logs/
*.log
coverage/
*.tsbuildinfo
.tsbuild-node/

# Classroom relay crash-backups (COORDINATOR_BUILD_PLAN §5). Ciphertext, useless
# without the instructor key, but it is student work and never belongs in the repo.
classroom-runs/

# Local-only instructor unlock material — never commit.
local-secrets/

# Local agent/workspace artifacts
.agents/
.claude/
.codex/
HANDOFF.md
START_HERE.md
TASKS.md
PROJECT_PLAN.md
Build Plan for Autonomous Drone Mission Simulator.txt

# Launch/GTM kit + packaged distributables — built locally, not part of the source repo
outputs/
.vercel
.env*
# Vercel CLI appends .env* above — keep the example tracked (rule order matters)
!.env.example
